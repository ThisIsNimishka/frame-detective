"""
app/  — Frame Detective application package

Structure
---------
app/
  models/       Data models (missions, quizzes, game state)
  views/        Page renderers (one per HTML page)
  templates/    HTML template builders (base shell, components)
  static/
    css/        shared.css
    js/         shared.js (localStorage state)
  server.py     HTTP server + router
"""
